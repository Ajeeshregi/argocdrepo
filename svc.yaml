apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: ajeeshregi
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
  - port: 80  #service port inside
    targetPort: 80 #container port
